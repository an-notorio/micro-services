server:
  port: 8888

spring:
  profiles:
    active: docker
  application:
    name: config-server
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations-docker